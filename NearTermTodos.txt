## Immediate — End-to-End Verification

- [x] Fix kprobe hook to resolve correct kernel symbols (__pte_offset_map_lock, pte_mkwrite_novma)
- [x] Implement inline PTE helpers for x86-64 (set_pte_at → WRITE_ONCE, mk_pte → pfn_pte, pte_unmap_unlock)
- [x] Run VM end-to-end test: kprobe registered, symbols resolved, compression/decompression roundtrip
- [x] Add E2E test suite (minimem_e2e_test.sh) for hook resolution, min_savings_pct, max_pool_pages, scanner

## Near-Term — Kernel Module (Stage 1 completion)

- [ ] Test transparent page fault decompression end-to-end (allocate memory → scanner compresses → access triggers decompress → verify data)
- [ ] Test scanner idle page detection with CONFIG_PAGE_IDLE_FLAG (currently skipped in Alpine VM)
- [ ] Build custom kernel with MiniMem patches applied (PTE_MARKER_MINIMEM in handle_pte_marker)
- [ ] Verify shrinker actually frees memory under pressure

## Algorithm Library (Stage 0 remaining)

- [ ] SIMD paths for LZ4 (SSE2/AVX2)
- [ ] Zstd dictionary mode (add dictionary training for homogeneous pages)
- [ ] Real data benchmarks (collect actual page dumps + safetensor weights)

## Library Packaging

- [ ] Install target verification on clean system
- [ ] CI/CD for library build and kernel module build

## VRAM Prototype (Milestone 3)

- [ ] CUDA userspace program: load LLM weights, compress cold tensors with libminimem
- [ ] Tiered VRAM management (hot/warm/cold/frozen)
- [ ] MoE sparse activation: use router to determine hot experts