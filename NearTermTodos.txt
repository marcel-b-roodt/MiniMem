## Near-Term — Completed (this session)

- [x] Fix parallel decompression crash in zs_obj_read_begin
  - Root cause: race condition between shrinker/drain freeing zsmalloc handles
    and parallel workers accessing them
  - Fix: rw_semaphore on minimem_map (read lock for parallel decompress,
    write lock for drain/remove/remove_all)
  - Added NULL return checks for zs_obj_read_begin in all paths
- [x] Fix build-kmod.sh Makefile heredoc — $(src) and $(PWD) expanded by shell
  instead of Make; changed to single-quoted heredoc with sed substitution
- [x] Fix vm-test-minimem.sh — wrong rebuild command for custom kernel,
  added ensure_module_loaded() helper between test sections
- [x] Fix format string warning in minimem_compat_check.c (%u → %lu)
- [x] All 8GB VM tests pass with custom kernel (73 kselftest, 17 E2E, 6 perf harness)
- [x] Fix "unrecognized swap entry" flood
  - Root cause: stale kernel build didn't include CONFIG_MINIMEM zap_pte_range patch
  - After properly rebuilding kernel, the CONFIG_MINIMEM code path catches MiniMem
    PTE markers in zap_pte_range and calls minimem_zap_cb
  - Added zap_cb_count and zap_cb_miss_count sysfs attributes for monitoring
  - Debug logging in minimem_zswap_zap_cb (pr_debug + first-call pr_info)
- [x] Verify "unrecognized swap entry" encoding analysis
  - Entries are MiniMem PTE markers (swp_type=31 in arch-independent format)
  - The previous "swp_type=15" analysis was wrong — SWP_TYPE_SHIFT is 58 (not 59)
    on x86-64 because BITS_PER_XA_VALUE = BITS_PER_LONG - 1 = 63

## Near-Term — Open Issues

- [ ] CPU overhead test threshold too strict for VMs
  - Scanner rate 132-133 pages/s in QEMU VM (4 vCPU, 8GB)
  - Test threshold: 1000 pages/s for "OK", 10000 for "PASS"
  - Need to either: lower VM test threshold, or adjust test for virtualized env
  - Real hardware likely performs much better (10-100x faster page scanning)

## Near-Term — Kernel Module (Critical)

- [ ] Test on real hardware (not QEMU) — NUMA, large memory, different CPU arches
- [ ] Verify shrinker under real memory pressure
- [ ] THP + KSM compatibility testing (kernel upstreaming prerequisite)
- [ ] OOM behavior verification (kernel upstreaming prerequisite)
- [ ] zram coexistence load testing (kernel upstreaming prerequisite)

## Near-Term — VM Testing

- [ ] E2E test: large memory VM (4GB+)